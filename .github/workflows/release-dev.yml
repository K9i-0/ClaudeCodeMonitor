name: Development Release

on:
  push:
    branches:
      - develop

permissions:
  contents: write

jobs:
  release:
    uses: ./.github/workflows/release-common.yml
    with:
      is_dev_build: true
      branch_name: develop
    secrets: inherit